Log parser for our nginx servers that will parse each line, filter based on the properties, and shoot the information over to statsd to aggregation and shipping.


Usage:

python /path/to/parse.py /path/to/access.log
